{
  "description": "Pre-commit hook configuration options",
  "options": {
    "fast": {
      "description": "Fast pre-commit checks (linting + build only)",
      "hook": ".husky/pre-commit-fast"
    },
    "standard": {
      "description": "Standard pre-commit checks (linting + unit/integration tests + build)",
      "hook": ".husky/pre-commit"
    },
    "full": {
      "description": "Full pre-commit checks (linting + all tests + build)",
      "hook": ".husky/pre-commit-full"
    }
  },
  "current": "standard",
  "usage": {
    "switch": "To switch between configurations, copy the desired hook to .husky/pre-commit",
    "examples": [
      "cp .husky/pre-commit-fast .husky/pre-commit  # For fast checks",
      "cp .husky/pre-commit .husky/pre-commit       # For standard checks (current)",
      "cp .husky/pre-commit-full .husky/pre-commit  # For full checks including E2E"
    ]
  }
}
