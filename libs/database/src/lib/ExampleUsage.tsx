// Example usage removed: this file contained references to non-exported
// helpers (`DatabaseProvider`, `useDatabase`) and caused type-check
// failures during CI. It previously demonstrated database operations
// but is intentionally left empty to avoid blocking builds.

export {};
