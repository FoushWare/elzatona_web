/**
 * Website-specific test mocks
 * v1.0
 *
 * Mocks specific to website application tests
 * Shared mocks are in the parent directory
 */

// Export website-specific mocks here
// Currently all mocks are shared, but this structure allows for website-specific mocks in the future

export * from "../shared-contexts"; // Re-export shared contexts for convenience
