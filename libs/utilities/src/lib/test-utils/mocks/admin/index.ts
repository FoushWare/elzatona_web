/**
 * Admin-specific test mocks
 * v1.0
 *
 * Mocks specific to admin application tests
 * Shared mocks are in the parent directory
 */

// Export admin-specific mocks here
// Currently all mocks are shared, but this structure allows for admin-specific mocks in the future

export * from "../shared-contexts"; // Re-export shared contexts for convenience
