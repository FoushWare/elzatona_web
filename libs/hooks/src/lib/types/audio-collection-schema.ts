// Type definitions for audio-collection-schema
// These types are used by shared-hooks but the service is app-specific

export interface QuestionAudioMapping {
  questionId: string;
  audioUrl: string;
}
