name: CodeQL Analysis

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Monday at 00:00 UTC
    - cron: "0 0 * * 1"
  workflow_dispatch:

# Cancel in-progress runs on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    timeout-minutes: 45 # Further reduced timeout to avoid rate limit issues
    permissions:
      contents: read
      security-events: write
      # Minimize permissions to reduce API calls
      # actions: read - Remove to reduce telemetry API calls
      # pull-requests: read - Remove to reduce API calls

    strategy:
      fail-fast: false
      matrix:
        # CodeQL treats JavaScript and TypeScript as one language
        # Using just "javascript" will analyze both .js and .ts files
        language: ["javascript"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          # Use valid query packs for CodeQL v2.23.8
          queries: security-extended
          # Use custom config to reduce scanning scope
          config-file: .github/workflows/codeql-config.yml

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          # Cache omitted - project uses bun.lock, not package-lock.json
          # Omitting cache parameter disables caching by default

      - name: Install dependencies
        run: npm install
        # Using npm install instead of npm ci since package-lock.json doesn't exist
        # Project uses bun as primary package manager

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4
        # CodeQL needs to build the code to analyze it properly
        # This step automatically detects and runs the build command

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
          # Completely disable all API-intensive features
          upload: false
          output_file_path: false
          wait-for-processing: false
          skip_database_upload: true
          # Additional flags to prevent API calls
          no_fingerprint: true
          threads: 1
          ram: 2048
