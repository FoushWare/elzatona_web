name: Test Admin Dashboard Page

on:
  push:
    branches: [main, develop, "release/**"]
    paths:
      - "apps/website/src/app/admin/dashboard/**"
      - "libs/common-ui/src/components/templates/AdminDashboardTemplate.*"
      - "libs/common-ui/src/components/organisms/AdminStatsGrid.*"
      - "libs/common-ui/src/components/organisms/AdminQuickActionsSection.*"
      - "libs/common-ui/src/components/molecules/AdminMetricCard.*"
      - ".github/workflows/test-admin-dashboard.yml"
  pull_request:
    branches: [main, develop, "release/**"]
    paths:
      - "apps/website/src/app/admin/dashboard/**"
      - "libs/common-ui/src/components/templates/AdminDashboardTemplate.*"
      - "libs/common-ui/src/components/organisms/AdminStatsGrid.*"
      - "libs/common-ui/src/components/organisms/AdminQuickActionsSection.*"
      - "libs/common-ui/src/components/molecules/AdminMetricCard.*"
      - ".github/workflows/test-admin-dashboard.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-admin-dashboard:
    name: Test Admin Dashboard Page
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Run unit tests for Admin Dashboard
        run: |
          echo "🧪 Running unit tests for Admin Dashboard page..."
          NODE_OPTIONS=--max-old-space-size=768 npx jest \
            apps/website/src/app/admin/dashboard/page.test.tsx \
            --maxWorkers=1 \
            --silent \
            --coverage=false \
            --config=apps/website/jest.config.js
        continue-on-error: false
        env:
          NODE_OPTIONS: "--max-old-space-size=768"
          CI: "true"
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY || 'placeholder-key' }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.TEST_SUPABASE_SERVICE_ROLE_KEY || 'placeholder-service-role-key' }}
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'placeholder-secret' }}
          ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL || 'test@example.com' }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD || 'test-password' }}

      - name: Run integration tests for Admin Dashboard
        run: |
          echo "🔗 Running integration tests for Admin Dashboard..."
          NODE_OPTIONS=--max-old-space-size=768 npx jest \
            apps/website/src/app/admin/dashboard/page.integration.test.tsx \
            --maxWorkers=1 \
            --silent \
            --coverage=false \
            --config=apps/website/jest.config.js || echo "⚠️  Integration tests not found, skipping"
        continue-on-error: true
        env:
          NODE_OPTIONS: "--max-old-space-size=768"
          CI: "true"
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY || 'placeholder-key' }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.TEST_SUPABASE_SERVICE_ROLE_KEY || 'placeholder-service-role-key' }}
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'placeholder-secret' }}
          ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL || 'test@example.com' }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD || 'test-password' }}

      - name: Start development server for E2E tests
        run: |
          echo "🚀 Starting development server for E2E tests..."
          npm run dev:light:test &
          sleep 15
          echo "✅ Development server started"
        continue-on-error: true
        env:
          APP_ENV: "test"
          NEXT_PUBLIC_APP_ENV: "test"
          NODE_ENV: "test"
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY || 'placeholder-key' }}
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'placeholder-secret' }}
          ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL || 'test@example.com' }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD || 'test-password' }}

      - name: Run E2E tests for Admin Dashboard
        run: |
          echo "🌐 Running E2E tests for Admin Dashboard..."
          npx playwright test apps/website/tests/e2e/admin-dashboard/admin-dashboard.spec.ts --project=chromium || echo "⚠️  E2E tests failed or not found"
        continue-on-error: true
        env:
          CI: "true"
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY || 'placeholder-key' }}
          JWT_SECRET: ${{ secrets.JWT_SECRET || 'placeholder-secret' }}
          ADMIN_EMAIL: ${{ secrets.ADMIN_EMAIL || 'test@example.com' }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD || 'test-password' }}

      - name: Update snapshot tests
        run: |
          echo "📸 Updating snapshot tests for Admin Dashboard..."
          NODE_OPTIONS=--max-old-space-size=768 npx jest \
            apps/website/src/app/admin/dashboard/page.test.tsx \
            --updateSnapshot \
            --maxWorkers=1 \
            --silent \
            --config=apps/website/jest.config.js || echo "⚠️  Snapshot update skipped"
        continue-on-error: true
        env:
          NODE_OPTIONS: "--max-old-space-size=768"
          CI: "true"
