// Minimal helper stub for homepage integration tests
export const getPersonalizedContent = async () => ({ data: [], meta: {} });

const homePageHelpers = { getPersonalizedContent };
export default homePageHelpers;
